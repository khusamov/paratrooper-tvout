#include "Status.h"
#include "Constants.h"
#include "MemoryFree.h"

void Status::draw() {
	// this->tv->print(3, 3, "FIRE: ");
	// this->tv->print(fireCounter);
	// this->tv->print(3, 10, "BOMB: ");
	// this->tv->print(bombCounter);
	// this->tv->print(SCREEN_HEIGHT / 2, 3, "RAM: ");
	// this->tv->print(freeMemory());
}